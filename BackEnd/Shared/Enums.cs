namespace Shared.Enums
{
    // This is simply an example to illustrate where to place
    // enumerated types within this WebAPI
    //
    public enum ApplicationResult
    {
        Failed = 0,
        Success = 1
    }
}